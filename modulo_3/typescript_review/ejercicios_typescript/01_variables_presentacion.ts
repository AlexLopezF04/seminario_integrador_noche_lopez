// Con tipo explícito
const nombre1: string  = "Alex Lopez";
const edad1:   number  = 26;
const activo1: boolean = true;

// Sin tipo — TypeScript lo infiere automáticamente del valor
const nombre2: string  = "Alex Lopez";   // TypeScript sabe que es string
const edad2:   number  = 26;      // TypeScript sabe que es number
const activo2: boolean = true;    // TypeScript sabe que es boolean